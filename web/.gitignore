#############################
# Web (Vite / React / TS)   #
#############################

# Dependencies
node_modules/

# Yarn PnP / modern package managers artifacts
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
!.yarn/sdks
!.yarn/versions

# Package manager logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# Testing / Coverage
coverage/

# Build outputs
dist/
build/

# Misc system files
.DS_Store
Thumbs.db

# SSL / keys (keep sample certs separately if needed)
*.pem
*.crt
*.key

# Environment files (commit only samples)
.env
.env.*
!.env.sample

# Vercel / deployment
.vercel/

# TypeScript incremental build info
*.tsbuildinfo

# IDE / editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# Generated by tools / frameworks (not needed in Vite now)
next-env.d.ts

# Cache
.parcel-cache
.eslintcache
*.cache
cache/

# Storybook / other tool outputs (if added later)
storybook-static/

# Optional TODO notes
todo.md

# Local temp files
tmp/
*.local
